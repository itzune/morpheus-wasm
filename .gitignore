# Model binaries (~55 MB each) — never commit; load from HF in production
# or regenerate locally with: python3 test/patch-headcount.py
model/*.gguf

# Test screenshots
test/*.png

# Node / tooling
node_modules/
package-lock.json

# Editor / OS cruft
.DS_Store
*.swp
